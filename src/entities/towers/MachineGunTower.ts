import { Tower } from '@/entities/Tower';

/**
 * Machine Gun — high-fire-rate kinetic tower. Fires the same `bullet`
 * projectile as the Bullet Turret but at much higher cadence and slightly
 * lower per-shot damage. Strong vs swarms.
 */
export class MachineGunTower extends Tower {}
